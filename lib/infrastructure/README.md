# infrastructure

Здесь живут реализации интерфейсов, объявленных в `domain`: `pdfrx`, `drift`,
`flutter_gemma`, Drive API, HTTP-клиент прокси. Слой заполняется начиная
с сессии S2 — сейчас пуст намеренно, чтобы структура была видна с первого
коммита.
